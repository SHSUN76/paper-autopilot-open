# reference/

참고 논문 + metadata + 차용 logic.

```
reference/
└── Wang2023_NMC_surface/
    ├── original.pdf
    ├── metadata.md   ← /paper-access auto-extract
    └── logic.md      ← 차용할 전개 로직
```

**Note**: `[YYMMDD_내용]` 대신 `[paper-slug]/` 사용. paper-access skill에 PDF 던지면 metadata.md 자동 작성.

CLAUDE.md 허브 링크: `[Wang2023 NMC surface](./reference/Wang2023_NMC_surface/) — Intro 전개 차용`
